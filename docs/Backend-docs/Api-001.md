# Where Is My Slot - Backend API Documentation

**Base URL**: `https://api.whereslot.shamveelp.xyz`

## Authentication

Most protected endpoints require a Bearer token in the `Authorization` header.

**Header Format:**
```
Authorization: Bearer <your_jwt_access_token>
```

---

## 1. System & Health

### 1.1 Health Check
Check if the API is running.
- **URL**: `/api/v1/health`
- **Method**: `GET`
- **Auth Required**: No
- **Response Format**:
  ```json
  {
    "status": "string (e.g. 'ok where slot')",
    "timestamp": "string (ISO Date)",
    "version": "string"
  }
  ```

### 1.2 Seed Database
Seed the database with initial test data (User, Category, Business).
- **URL**: `/api/v1/seed`
- **Method**: `POST`
- **Auth Required**: No
- **Response Format**: Returns the seeded `user`, `category`, and `business` objects.

---

## 2. User Authentication (`/api/v1/auth`)

### 2.1 Send OTP
Sends a 6-digit OTP to the user's email for verification.
- **URL**: `/api/v1/auth/send-otp`
- **Method**: `POST`
- **Auth Required**: No
- **Request Body**:
  ```json
  {
    "email": "string (required, valid email)"
  }
  ```
- **Response Format**: 
  ```json
  {
    "success": "boolean",
    "data": {
      "message": "string"
    }
  }
  ```

### 2.2 Verify OTP
Verifies the OTP sent to the user's email.
- **URL**: `/api/v1/auth/verify-otp`
- **Method**: `POST`
- **Auth Required**: No
- **Request Body**:
  ```json
  {
    "email": "string (required)",
    "otp": "string (required, exactly 6 characters)"
  }
  ```
- **Response Format**: 
  ```json
  {
    "success": "boolean",
    "data": {
      "message": "string (contains JWT token on success)"
    }
  }
  ```

### 2.3 User Login
Login with email and password.
- **URL**: `/api/v1/auth/login`
- **Method**: `POST`
- **Auth Required**: No
- **Request Body**:
  ```json
  {
    "email": "string (required, valid email)",
    "password": "string (required)"
  }
  ```
- **Response Format**:
  ```json
  {
    "success": "boolean",
    "data": {
      "accessToken": "string (JWT)",
      "user": {
        "id": "string",
        "name": "string",
        "username": "string (optional)",
        "email": "string",
        "isPasswordSet": "boolean",
        "role": "string (USER or BUSINESS or SUPER_ADMIN)",
        "isActive": "boolean",
        "createdAt": "string (ISO Date)",
        "updatedAt": "string (ISO Date)"
      }
    }
  }
  ```

### 2.4 User Register
Register a new user account. Requires a verified OTP in the payload.
- **URL**: `/api/v1/auth/register`
- **Method**: `POST`
- **Auth Required**: No
- **Request Body**:
  ```json
  {
    "firstName": "string (required)",
    "lastName": "string (required)",
    "username": "string (required, min 3 chars, alphanumeric)",
    "email": "string (required, valid email)",
    "password": "string (required, min 6 chars)",
    "otp": "string (required, exactly 6 characters)"
  }
  ```
- **Response Format**: Returns the same structure as `Login` (`accessToken` and `user`).

### 2.5 Check Availability
Check if a username or email is already taken.
- **URL**: `/api/v1/auth/check-availability`
- **Method**: `GET`
- **Query Params**: `?username=string` OR `?email=string`
- **Auth Required**: No
- **Response Format**:
  ```json
  {
    "success": "boolean",
    "data": {
      "available": "boolean"
    }
  }
  ```

### 2.6 Get Current User (Me)
Get the profile of the currently authenticated user.
- **URL**: `/api/v1/auth/me`
- **Method**: `GET`
- **Auth Required**: Yes (Bearer Token)
- **Response Format**: Returns the `user` object (see Login response).

### 2.7 Set Password
Change or set the user's password.
- **URL**: `/api/v1/auth/set-password`
- **Method**: `POST`
- **Auth Required**: Yes (Bearer Token)
- **Request Body**:
  ```json
  {
    "newPassword": "string (required, min 6 chars)",
    "oldPassword": "string (optional, required if password was already set)" 
  }
  ```
- **Response Format**: 
  ```json
  {
    "success": "boolean",
    "data": {
      "message": "string"
    }
  }
  ```

---

## 3. Business Authentication (`/api/v1/business-auth`)

### 3.1 Send Business OTP
Send an OTP to a business email for registration/onboarding.
- **URL**: `/api/v1/business-auth/send-otp`
- **Method**: `POST`
- **Auth Required**: No
- **Request Body**:
  ```json
  {
    "email": "string (required)"
  }
  ```
- **Response Format**:
  ```json
  {
    "success": "boolean",
    "data": {
      "message": "string"
    }
  }
  ```

### 3.2 Verify Business OTP
Verify the OTP for business registration. Returns a short-lived session token used for onboarding.
- **URL**: `/api/v1/business-auth/verify-otp`
- **Method**: `POST`
- **Auth Required**: No
- **Request Body**:
  ```json
  {
    "email": "string (required)",
    "otp": "string (required, exactly 6 characters)"
  }
  ```
- **Response Format**: 
  ```json
  {
    "success": "boolean",
    "data": {
      "verified": "boolean",
      "verifiedToken": "string (JWT valid for 2h)"
    }
  }
  ```

### 3.3 Business Login
Login as a merchant/business owner.
- **URL**: `/api/v1/business-auth/login`
- **Method**: `POST`
- **Auth Required**: No
- **Request Body**:
  ```json
  {
    "email": "string (required, valid email)",
    "password": "string (required)"
  }
  ```
- **Response Format**: Returns `accessToken` and `business` (see section 4 for business schema).

### 3.4 Business Set Password
Change or set the merchant password.
- **URL**: `/api/v1/business-auth/set-password`
- **Method**: `POST`
- **Auth Required**: Yes (Bearer Token)
- **Request Body**: Same as User Set Password.

---

## 4. Businesses (`/api/v1/businesses`)

**Business Object Schema (Response):**
```json
{
  "id": "string",
  "ownerId": "string (User ID)",
  "contactEmail": "string",
  "isPasswordSet": "boolean",
  "name": "string",
  "username": "string (optional)",
  "tagline": "string (optional)",
  "description": "string (optional)",
  "establishedYear": "number (optional)",
  "gstNumber": "string (optional)",
  "contactPerson": "string (optional)",
  "phone": "string (optional)",
  "email": "string (optional)",
  "websiteUrl": "string (optional)",
  "whatsappNumber": "string (optional)",
  "mobileNumbers": ["string"],
  "landlineNumbers": ["string"],
  "emails": ["string"],
  "address": "string (optional)",
  "pincode": "string (optional)",
  "plotNo": "string (optional)",
  "buildingName": "string (optional)",
  "streetName": "string (optional)",
  "landmark": "string (optional)",
  "area": "string (optional)",
  "city": "string (optional)",
  "state": "string (optional)",
  "latitude": "number (optional)",
  "longitude": "number (optional)",
  "googleMapsUrl": "string (optional)",
  "timings": { "type": "object (mixed)" },
  "primaryCategory": "string (optional)",
  "subCategories": ["string"],
  "amenities": ["string"],
  "parking": { "type": "object (mixed)" },
  "logo": "string (optional)",
  "coverPhoto": "string (optional)",
  "images": ["string"],
  "socialLinks": { "type": "object (mixed)" },
  "isVerified": "boolean",
  "status": "string (PENDING, APPROVED, REJECTED)",
  "createdAt": "string (ISO Date)",
  "updatedAt": "string (ISO Date)"
}
```

### 4.1 Explore Businesses
Get a list of approved businesses.
- **URL**: `/api/v1/businesses/explore`
- **Method**: `GET`
- **Auth Required**: No
- **Response Format**:
  ```json
  {
    "success": "boolean",
    "data": [
      { /* Business Object */ }
    ]
  }
  ```

### 4.2 Get My Business
Get the business profile owned by the authenticated merchant.
- **URL**: `/api/v1/businesses/me`
- **Method**: `GET`
- **Auth Required**: Yes (Bearer Token, Role: `BUSINESS` or `SUPER_ADMIN`)
- **Response Format**:
  ```json
  {
    "success": "boolean",
    "data": { /* Business Object */ }
  }
  ```

### 4.3 Update My Business
Update the business profile of the authenticated merchant.
- **URL**: `/api/v1/businesses/me`
- **Method**: `PATCH`
- **Auth Required**: Yes (Bearer Token, Role: `BUSINESS` or `SUPER_ADMIN`)
- **Request Body**: Any valid fields from the Business Object (except `id`, `ownerId`, `status`, etc.).
- **Response Format**: Returns updated Business Object.

### 4.4 Onboard Business
Submit business details to complete the registration/onboarding process.
- **URL**: `/api/v1/businesses/onboard`
- **Method**: `POST`
- **Auth Required**: Yes (Typically requires the `verifiedToken` from OTP verification via Bearer token)
- **Request Body**:
  ```json
  {
    "name": "string (required)",
    "description": "string (required)",
    "email": "string",
    "phone": "string",
    "address": "string (required)",
    "timings": "object",
    "parking": "object",
    "images": ["string"]
  }
  ```
- **Response Format**: Returns `{ "business": { /* Business Object */ }, "accessToken": "string" }`

### 4.5 Upload Image
Upload an image to Cloudinary.
- **URL**: `/api/v1/businesses/upload-image`
- **Method**: `POST`
- **Auth Required**: No
- **Headers**: `Content-Type: multipart/form-data`
- **Request Body**: Form-data with key `file` (the image file).
- **Response Format**: 
  ```json
  {
    "success": "boolean",
    "data": {
      "url": "string (Cloudinary Image URL)"
    }
  }
  ```

---

## 5. Admin (`/api/v1/admin`)

*Note: All Admin routes require a Bearer Token with the `SUPER_ADMIN` role.*

### 5.1 Get All Businesses
Retrieve all businesses (pending, approved, rejected).
- **URL**: `/api/v1/admin/businesses`
- **Method**: `GET`
- **Auth Required**: Yes (`SUPER_ADMIN`)
- **Response Format**: Array of Business Objects.

### 5.2 Add Business (Admin)
Create a new business directly as an admin.
- **URL**: `/api/v1/admin/businesses/add`
- **Method**: `POST`
- **Auth Required**: Yes (`SUPER_ADMIN`)
- **Request Body**: Partial Business Object.
- **Response Format**: Returns created Business Object.

### 5.3 Approve Business
Approve a pending business registration.
- **URL**: `/api/v1/admin/businesses/:id/approve`
- **Method**: `POST`
- **Auth Required**: Yes (`SUPER_ADMIN`)
- **Response Format**: Returns updated Business Object.

### 5.4 Reject Business
Reject a pending business registration.
- **URL**: `/api/v1/admin/businesses/:id/reject`
- **Method**: `POST`
- **Auth Required**: Yes (`SUPER_ADMIN`)
- **Response Format**: Returns updated Business Object.

---

## 6. Posts (`/api/v1/posts`)

**Post Object Schema (Response):**
```json
{
  "id": "string",
  "text": "string",
  "image": "string (optional)",
  "tags": ["string"],
  "location": "string (optional)",
  "views": "number",
  "businessId": "string",
  "createdAt": "string (ISO Date)",
  "updatedAt": "string (ISO Date)"
}
```

### 6.1 Get Posts
Retrieve a feed of posts.
- **URL**: `/api/v1/posts`
- **Method**: `GET`
- **Auth Required**: No
- **Response Format**:
  ```json
  {
    "success": "boolean",
    "data": [ { /* Post Object */ } ]
  }
  ```

### 6.2 Create Post
Create a new post.
- **URL**: `/api/v1/posts`
- **Method**: `POST`
- **Auth Required**: Yes (Bearer Token, typically Role: `BUSINESS`)
- **Request Body**:
  ```json
  {
    "text": "string (required)",
    "image": "string (optional)",
    "tags": ["string (optional)"],
    "location": "string (optional)"
  }
  ```
- **Response Format**: Returns created Post Object.

### 6.3 Update Post
Update an existing post.
- **URL**: `/api/v1/posts/:id`
- **Method**: `POST`
- **Auth Required**: Yes (Bearer Token)
- **Request Body**: Partial fields from Create Post request.
- **Response Format**: Returns updated Post Object.

### 6.4 Delete Post
Delete an existing post.
- **URL**: `/api/v1/posts/:id`
- **Method**: `DELETE`
- **Auth Required**: Yes (Bearer Token)
- **Response Format**:
  ```json
  {
    "success": "boolean",
    "data": { "message": "string" }
  }
  ```

### 6.5 Toggle Like
Like or unlike a post.
- **URL**: `/api/v1/posts/:id/like`
- **Method**: `POST`
- **Auth Required**: Yes (Bearer Token)
- **Response Format**:
  ```json
  {
    "success": "boolean",
    "data": { "message": "string (Liked or Unliked)" }
  }
  ```

### 6.6 Add Comment
Add a comment to a post.
- **URL**: `/api/v1/posts/:id/comments`
- **Method**: `POST`
- **Auth Required**: Yes (Bearer Token)
- **Request Body**:
  ```json
  {
    "text": "string (required)"
  }
  ```
- **Response Format**:
  ```json
  {
    "success": "boolean",
    "data": {
      "id": "string",
      "postId": "string",
      "userId": "string",
      "text": "string",
      "createdAt": "string (ISO Date)"
    }
  }
  ```

### 6.7 Get Comments
Get all comments for a post.
- **URL**: `/api/v1/posts/:id/comments`
- **Method**: `GET`
- **Auth Required**: No
- **Response Format**: Array of Comment Objects.

### 6.8 Delete Comment
Delete a comment on a post.
- **URL**: `/api/v1/posts/comments/:id`
- **Method**: `DELETE`
- **Auth Required**: Yes (Bearer Token)

---

## 7. Flash Deals (`/api/v1/flash-deals`)

**FlashDeal Object Schema (Response):**
```json
{
  "id": "string",
  "businessId": "string",
  "offer": "string",
  "image": "string",
  "type": "string (default: 'DISCOUNT')",
  "navigateLink": "string (optional)",
  "activeUntil": "string (ISO Date, optional)",
  "createdAt": "string (ISO Date)"
}
```

### 7.1 Get Flash Deals
Retrieve active flash deals.
- **URL**: `/api/v1/flash-deals`
- **Method**: `GET`
- **Auth Required**: No
- **Response Format**:
  ```json
  {
    "success": "boolean",
    "data": [ { /* FlashDeal Object */ } ]
  }
  ```

### 7.2 Create Flash Deal
Create a new flash deal.
- **URL**: `/api/v1/flash-deals`
- **Method**: `POST`
- **Auth Required**: Yes (Bearer Token, typically Role: `BUSINESS`)
- **Request Body**:
  ```json
  {
    "offer": "string (required)",
    "image": "string (required)",
    "type": "string (optional)",
    "navigateLink": "string (optional)",
    "activeUntil": "string (ISO Date, optional)"
  }
  ```
- **Response Format**: Returns created FlashDeal Object.

---

## 8. Location (`/api/v1/location`)

### 8.1 Search Location
Search for a location using a text query (e.g., "Indiranagar, Bangalore"). Uses Nominatim Geocoding API.
- **URL**: `/api/v1/location/search`
- **Method**: `GET`
- **Query Params**: `?query=string`
- **Auth Required**: No
- **Response Format**:
  ```json
  {
    "success": "boolean",
    "data": {
      "results": [
        {
          "address": "string",
          "latitude": "number",
          "longitude": "number"
        }
      ]
    }
  }
  ```

### 8.2 Set User Location
Set the current geographical location of the authenticated user.
- **URL**: `/api/v1/location/set`
- **Method**: `POST`
- **Auth Required**: Yes (Bearer Token)
- **Request Body**:
  ```json
  {
    "address": "string (required)",
    "latitude": "number (required)",
    "longitude": "number (required)"
  }
  ```
- **Response Format**:
  ```json
  {
    "success": "boolean",
    "data": {
      "message": "string"
    }
  }
  ```
